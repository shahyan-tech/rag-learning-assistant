apiVersion: v1
kind: Service
metadata:
  name: rag-frontend-service
  namespace: rag-learning
spec:
  type: LoadBalancer
  selector:
    app: rag-frontend
  ports:
    - name: http
      port: 5173
      targetPort: 80
