apiVersion: v1
kind: Service
metadata:
  name: rag-backend-service
  namespace: rag-learning
spec:
  type: LoadBalancer
  selector:
    app: rag-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
